Find collocations for the following {source_language_name} lemmas.

Words to analyze:
{items_json}

For each {source_language_name} lemma provided, find common {source_language_name} collocations or phrases that include the lemma.

Output JSON as an object where keys are the UIDs and values are objects with:
- "collocations": A JSON list of 0-3 short collocations in {source_language_name} that commonly use the lemma

Respond with valid JSON. No additional text.
